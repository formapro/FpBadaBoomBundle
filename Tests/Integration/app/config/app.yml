framework:
    secret:                                               sdkfj
    charset:                                              UTF-8
    test:                                                 true
    router:                                               { resource: "%kernel.root_dir%/config/routing.yml" }
    templating:
        engines:                                          [ 'php' ]
    session:
        storage_id:                                       session.storage.filesystem